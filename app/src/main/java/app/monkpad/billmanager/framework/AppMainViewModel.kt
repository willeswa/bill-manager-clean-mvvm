package app.monkpad.billmanager.framework

import android.app.Application
import androidx.lifecycle.AndroidViewModel

open class AppMainViewModel(application: Application): AndroidViewModel(application) {
}